import { styled } from "styled-components";

export const ContainerCard = styled.main`
   display: flex;
   justify-content: center;
   align-items: center;
   flex-direction: column;
   width: 500px;
   border-radius: 10px;
   color: #000;
   background-color: #888;
   margin-top: 25px;
   flex-wrap: wrap;
`;

export const Cep = styled.h2`
  margin: 16px 0;
  font-size: 36px;
`

export const Complementos = styled.span`
  margin-bottom: 25px;
  font-size: 20px;
  font-weight: 800;
`